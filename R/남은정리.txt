R
	chap01
		pdf 출력
		요인형 변환 (순서있는 요인형 순서없는 요인형)
		par(c(1,1)) - plot 출력 1행 1열
		par(c(1,2)) - plot 출력 1행 2열
		args(함수명) - 함수 매개변수 설명
	chap02
		벡터 데이터 생성
			입력할때 숫자와 문자열 혼합 -> 모두 문자열로 변환
		명령문 종결자 - 한줄에 여러개의 명령문 사용할 때만 사용
		매개변수> col.names=열이름 벡터
		names()
	chap07
		끝부분 cvFolds() - 모르겠다
